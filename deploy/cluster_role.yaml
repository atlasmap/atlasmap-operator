apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: atlasmap-operator
rules:
- apiGroups:
  - console.openshift.io
  resources:
  - consoleyamlsamples
  verbs:
  - get
  - create
  - update
  - delete
