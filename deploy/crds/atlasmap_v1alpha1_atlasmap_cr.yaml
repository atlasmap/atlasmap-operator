apiVersion: atlasmap.io/v1alpha1
kind: AtlasMap
metadata:
  name: example-atlasmap
spec:
  # The number of desired replicas
  replicas: 1
  # The AtlasMap image to use
  image: docker.io/atlasmap/atlasmap:latest
